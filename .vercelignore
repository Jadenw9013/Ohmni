# Only apps/web is served (see outputDirectory in vercel.json). Everything else
# is backend, tooling or generated output, and build/ is both enormous and
# partly unreadable, which would fail a CLI deploy.
.git
.venv
build/
out/
docs/
tests/
fixtures/
src/
scripts/
.ai/
**/__pycache__/
*.pyc
*.pyo
.pytest_cache/
